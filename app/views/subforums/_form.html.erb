<%= form_for(@subforum) do |f| %>
  <% if @subforum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subforum.errors.count, "error") %> prohibited this subforum from being saved:</h2>

      <ul>
      <% @subforum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
