
<table>
  <tr>
    <th>Name</th>
    <th>Desc</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subforums.each do |subforum| %>
  <tr>
    <td><%= subforum.name %></td>
    <td><%= subforum.desc %></td>
    <td><%= link_to 'Show', subforum %></td>
    <td><%= link_to 'Edit', edit_subforum_path(subforum) %></td>
    <td><%= link_to 'Destroy', subforum, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subforum', new_subforum_path %>
